# <a name="introduction"></a><span data-ttu-id="31bce-101">介绍</span><span class="sxs-lookup"><span data-stu-id="31bce-101">Introduction</span></span>

<span data-ttu-id="31bce-102">在 Microsoft&reg; Visual Basic&reg;编程语言是用于 Microsoft.NET Framework 的高级编程语言。</span><span class="sxs-lookup"><span data-stu-id="31bce-102">The Microsoft&reg; Visual Basic&reg; programming language is a high-level programming language for the Microsoft .NET Framework.</span></span> <span data-ttu-id="31bce-103">尽管它在设计上是一种易于使用，并轻松了解语言，还有功能强大，足以满足需求的经验丰富的程序员。</span><span class="sxs-lookup"><span data-stu-id="31bce-103">Although it is designed to be an approachable and easy-to-learn language, it is also powerful enough to satisfy the needs of experienced programmers.</span></span> <span data-ttu-id="31bce-104">Visual Basic 编程语言都具有一种语法类似于英语，提高清晰度和 Visual Basic 代码的可读性。</span><span class="sxs-lookup"><span data-stu-id="31bce-104">The Visual Basic programming language has a syntax that is similar to English, which promotes the clarity and readability of Visual Basic code.</span></span> <span data-ttu-id="31bce-105">如有可能，而不是缩写、 首字母缩写词或特殊字符使用有意义的词或短语。</span><span class="sxs-lookup"><span data-stu-id="31bce-105">Wherever possible, meaningful words or phrases are used instead of abbreviations, acronyms, or special characters.</span></span> <span data-ttu-id="31bce-106">外来或不需要的语法通常允许但不是必需的。</span><span class="sxs-lookup"><span data-stu-id="31bce-106">Extraneous or unneeded syntax is generally allowed but not required.</span></span>

<span data-ttu-id="31bce-107">Visual Basic 编程语言可以是强类型化或松散类型化的语言。</span><span class="sxs-lookup"><span data-stu-id="31bce-107">The Visual Basic programming language can be either a strongly typed or a loosely typed language.</span></span> <span data-ttu-id="31bce-108">松散类型化延迟很多类型检查，直到已运行程序的负担。</span><span class="sxs-lookup"><span data-stu-id="31bce-108">Loose typing defers much of the burden of type checking until a program is already running.</span></span> <span data-ttu-id="31bce-109">这包括不仅类型检查的转换，但还的方法调用，这意味着方法调用的绑定，可以延迟到运行时。</span><span class="sxs-lookup"><span data-stu-id="31bce-109">This includes not only type checking of conversions but also of method calls, meaning that the binding of a method call can be deferred until run-time.</span></span> <span data-ttu-id="31bce-110">构建原型或其他程序在其中的开发速度比执行速度更重要时，这很有用。</span><span class="sxs-lookup"><span data-stu-id="31bce-110">This is useful when building prototypes or other programs in which speed of development is more important than execution speed.</span></span> <span data-ttu-id="31bce-111">Visual Basic 编程语言还提供强类型化语义执行所有类型在编译时检查并不允许的方法调用运行时绑定。</span><span class="sxs-lookup"><span data-stu-id="31bce-111">The Visual Basic programming language also provides strongly typed semantics that performs all type checking at compile-time and disallows run-time binding of method calls.</span></span> <span data-ttu-id="31bce-112">这可以保证最大性能并有助于确保正确的类型转换。</span><span class="sxs-lookup"><span data-stu-id="31bce-112">This guarantees maximum performance and helps ensure that type conversions are correct.</span></span> <span data-ttu-id="31bce-113">生成的执行和执行的速度在正确性非常重要的生产应用程序时，这很有用。</span><span class="sxs-lookup"><span data-stu-id="31bce-113">This is useful when building production applications in which speed of execution and execution correctness is important.</span></span>

<span data-ttu-id="31bce-114">本文档介绍 Visual Basic 语言。</span><span class="sxs-lookup"><span data-stu-id="31bce-114">This document describes the Visual Basic language.</span></span> <span data-ttu-id="31bce-115">它旨在为完整的语言说明而不是语言教程或用户的参考手册。</span><span class="sxs-lookup"><span data-stu-id="31bce-115">It is meant to be a complete language description rather than a language tutorial or a user's reference manual.</span></span>

## <a name="grammar-notation"></a><span data-ttu-id="31bce-116">语法表示法</span><span class="sxs-lookup"><span data-stu-id="31bce-116">Grammar Notation</span></span>

<span data-ttu-id="31bce-117">本规范描述两个 grammar： 词法语法和句法的语法。</span><span class="sxs-lookup"><span data-stu-id="31bce-117">This specification describes two grammars: a lexical grammar and a syntactic grammar.</span></span> <span data-ttu-id="31bce-118">词法语法定义如何对窗体标记; 组合字符语法的语法定义如何对窗体 Visual Basic 程序组合令牌。</span><span class="sxs-lookup"><span data-stu-id="31bce-118">The lexical grammar defines how characters can be combined to form tokens; the syntactic grammar defines how the tokens can be combined to form Visual Basic programs.</span></span> <span data-ttu-id="31bce-119">此外，还有多个辅助语法，用于预处理操作，例如条件编译。</span><span class="sxs-lookup"><span data-stu-id="31bce-119">There are also several secondary grammars used for preprocessing operations like conditional compilation.</span></span>

<span data-ttu-id="31bce-120">此规范中的语法用 ANTLR 格式--请参阅 http://www.antlr.org/。</span><span class="sxs-lookup"><span data-stu-id="31bce-120">The grammars in this specification are written in ANTLR format -- see http://www.antlr.org/.</span></span>

<span data-ttu-id="31bce-121">用例是 Visual Basic 程序中并不重要。</span><span class="sxs-lookup"><span data-stu-id="31bce-121">Case is unimportant in Visual Basic programs.</span></span> <span data-ttu-id="31bce-122">为简单起见，所有终端会都提供标准大小写，但它们将匹配任何大小写。</span><span class="sxs-lookup"><span data-stu-id="31bce-122">For simplicity, all terminals will be given in standard casing, but any casing will match them.</span></span> <span data-ttu-id="31bce-123">由其对应的 ASCII 字符表示的是 ASCII 字符集中的可打印元素终端。</span><span class="sxs-lookup"><span data-stu-id="31bce-123">Terminals that are printable elements of the ASCII character set are represented by their corresponding ASCII characters.</span></span> <span data-ttu-id="31bce-124">Visual Basic 时也是不区分宽度匹配终端，允许全角字符 Unicode 字符，以匹配其半角的 Unicode 等效项，但只能在整个令牌基础上。</span><span class="sxs-lookup"><span data-stu-id="31bce-124">Visual Basic is also width insensitive when matching terminals, allowing full-width Unicode characters to match their half-width Unicode equivalents, but only on a whole-token basis.</span></span> <span data-ttu-id="31bce-125">如果它包含混合的半角和全宽字符，则令牌将不匹配。</span><span class="sxs-lookup"><span data-stu-id="31bce-125">A token will not match if it contains mixed half-width and full-width characters.</span></span>

<span data-ttu-id="31bce-126">换行符和缩进可能添加以提高可读性并不是生产环境的一部分。</span><span class="sxs-lookup"><span data-stu-id="31bce-126">Line breaks and indentation may be added for readability and are not part of the production.</span></span>

## <a name="compatibility"></a><span data-ttu-id="31bce-127">兼容性</span><span class="sxs-lookup"><span data-stu-id="31bce-127">Compatibility</span></span>

<span data-ttu-id="31bce-128">一种编程语言的一个重要功能是该语言的不同版本之间的兼容性。</span><span class="sxs-lookup"><span data-stu-id="31bce-128">An important feature of a programming language is compatibility between different versions of the language.</span></span> <span data-ttu-id="31bce-129">如果一种语言的较新版本不接受与以前版本的语言，相同的代码，或将其解释方式不同于以前的版本，然后负担时，可放置在上一名程序员他的代码从一个版本升级的语言到另一个。</span><span class="sxs-lookup"><span data-stu-id="31bce-129">If a newer version of a language does not accept the same code as a previous version of the language, or interprets it differently than the previous version, then a burden can be placed on a programmer when upgrading his code from one version of the language to another.</span></span> <span data-ttu-id="31bce-130">在这种情况下，版本之间的兼容性必须保留除时为语言使用者带来的好处是清晰且难以忍受性质。</span><span class="sxs-lookup"><span data-stu-id="31bce-130">As such, compatibility between versions must be preserved except when the benefit to language consumers is of a clear and overwhelming nature.</span></span>

<span data-ttu-id="31bce-131">以下策略控制对 Visual Basic 语言版本之间的更改。</span><span class="sxs-lookup"><span data-stu-id="31bce-131">The following policy governs changes to the Visual Basic language between versions.</span></span> <span data-ttu-id="31bce-132">术语语言，在此上下文中使用时仅指 Visual Basic 语言本身的语法和语义方面，不包括任何.NET Framework 类作为的一部分包括`Microsoft.VisualBasic`命名空间 （和子命名空间）。</span><span class="sxs-lookup"><span data-stu-id="31bce-132">The term language, when used in this context, refers only to the syntactic and semantic aspects of the Visual Basic language itself and does not include any .NET Framework classes included as a part of the `Microsoft.VisualBasic` namespace (and sub-namespaces).</span></span> <span data-ttu-id="31bce-133">本文档的作用域之外的单独版本控制和兼容性策略包含在.NET Framework 中的所有类。</span><span class="sxs-lookup"><span data-stu-id="31bce-133">All classes in the .NET Framework are covered by a separate versioning and compatibility policy outside the scope of this document.</span></span>

### <a name="kinds-of-compatibility-breaks"></a><span data-ttu-id="31bce-134">类型的兼容性中断</span><span class="sxs-lookup"><span data-stu-id="31bce-134">Kinds of compatibility breaks</span></span>

<span data-ttu-id="31bce-135">在理想情况下，兼容性将为现有版本的 Visual Basic 和 Visual Basic 的所有将来的版本之间的 100%。</span><span class="sxs-lookup"><span data-stu-id="31bce-135">In an ideal world, compatibility would be 100% between the existing version of Visual Basic and all future versions of Visual Basic.</span></span> <span data-ttu-id="31bce-136">但是，可能有其中需兼容性中断可能会超过了成本的情况下它可能会施加于程序员。</span><span class="sxs-lookup"><span data-stu-id="31bce-136">However, there may be situations where the need for a compatibility break may outweigh the cost it may impose on programmers.</span></span> <span data-ttu-id="31bce-137">这种情况下是：</span><span class="sxs-lookup"><span data-stu-id="31bce-137">Such situations are:</span></span>

* <span data-ttu-id="31bce-138">*新的警告。*</span><span class="sxs-lookup"><span data-stu-id="31bce-138">*New warnings.*</span></span> <span data-ttu-id="31bce-139">引入一个新的警告不、 本身，兼容性中断。</span><span class="sxs-lookup"><span data-stu-id="31bce-139">Introducing a new warning is not, per se, a compatibility break.</span></span> <span data-ttu-id="31bce-140">但是，许多开发人员使用"将警告视为错误"打开进行编译，因为额外必须格外小心引入警告时。</span><span class="sxs-lookup"><span data-stu-id="31bce-140">However, because many developers compile with "treat warnings as errors" turned on, extra care must be taken when introducing warnings.</span></span>

* <span data-ttu-id="31bce-141">*新的关键字。*</span><span class="sxs-lookup"><span data-stu-id="31bce-141">*New keywords.*</span></span> <span data-ttu-id="31bce-142">引入新语言功能时可能需要引入新的关键字。</span><span class="sxs-lookup"><span data-stu-id="31bce-142">Introducing new keywords may be necessary when introducing new language features.</span></span> <span data-ttu-id="31bce-143">将进行合理的努力，可以选择与用户的标识符发生冲突的可能性降至关键字还可以使用现有关键字非常有用。</span><span class="sxs-lookup"><span data-stu-id="31bce-143">Reasonable efforts will be made to choose keywords that minimize the possibility of collision with users' identifiers and to use existing keywords where it makes sense.</span></span> <span data-ttu-id="31bce-144">若要从以前版本升级项目和转义任何新的关键字，将提供帮助。</span><span class="sxs-lookup"><span data-stu-id="31bce-144">Help will be provided to upgrade projects from previous versions and escape any new keywords.</span></span>

* <span data-ttu-id="31bce-145">*编译器错误。*</span><span class="sxs-lookup"><span data-stu-id="31bce-145">*Compiler bugs.*</span></span> <span data-ttu-id="31bce-146">语言规范中有案可稽行为促使编译器的行为时，修复匹配有案可稽的行为的编译器行为可能有必要。</span><span class="sxs-lookup"><span data-stu-id="31bce-146">When the compiler's behavior is at odds with a documented behavior in the language specification, fixing the compiler behavior to match the documented behavior may be necessary.</span></span>

* <span data-ttu-id="31bce-147">*规范错误。*</span><span class="sxs-lookup"><span data-stu-id="31bce-147">*Specification bug.*</span></span> <span data-ttu-id="31bce-148">当编译器是与语言规范，但语言规范保持一致是明确的错误，更改语言规范，编译器行为可能有必要。</span><span class="sxs-lookup"><span data-stu-id="31bce-148">When the compiler is consistent with the language specification but the language specification is clearly wrong, changing the language specification and the compiler behavior may be necessary.</span></span> <span data-ttu-id="31bce-149">短语"显然是错误的"意味着有案可稽的行为与清晰而又明确大多数用户所期望的内容，并生成用户非常有必要行为。</span><span class="sxs-lookup"><span data-stu-id="31bce-149">The phrase "clearly wrong" means that the documented behavior runs counter to what a clear and unambiguous majority of users would expect and produces highly undesirable behavior for users.</span></span>

* <span data-ttu-id="31bce-150">*规范二义性。*</span><span class="sxs-lookup"><span data-stu-id="31bce-150">*Specification ambiguity.*</span></span> <span data-ttu-id="31bce-151">当语言规范应当清楚在特定情况下，会发生情况的内容，但不会和编译器处理这种情况是不一致或明确错误 （使用从上一个点相同的定义） 的方式、 澄清规范和更正的编译器行为可能有必要。</span><span class="sxs-lookup"><span data-stu-id="31bce-151">When the language specification should spell out what happens in a particular situation but doesn't, and the compiler handles the situation in a way that is either inconsistent or clearly wrong (using the same definition from the previous point), clarifying the specification and correcting the compiler behavior may be necessary.</span></span> <span data-ttu-id="31bce-152">换而言之，当规范包括用例 a、 b、 d 和 e，但省略任何提及的在 case c 中，会发生什么情况和在 c 中，则可能需要记录发生在 c 中大小写和更改以匹配的编译器行为的内容的情况下，编译器的行为不正确。</span><span class="sxs-lookup"><span data-stu-id="31bce-152">In other words, when the specification covers cases a, b, d and e, but omits any mention of what happens in case c, and the compiler behaves incorrectly in case c, it may be necessary to document what happens in case c and change the behavior of the compiler to match.</span></span> <span data-ttu-id="31bce-153">（请注意，如果该规范不明确的情况下会发生什么情况并和编译器的行为不明确问题时，编译器行为会变得实际上规范的方式。）</span><span class="sxs-lookup"><span data-stu-id="31bce-153">(Note that if the specification was ambiguous as to what happens in a situation and the compiler behaves in a manner that is not clearly wrong, the compiler behavior becomes the de facto specification.)</span></span>

* <span data-ttu-id="31bce-154">*使成为编译时错误的运行时错误。*</span><span class="sxs-lookup"><span data-stu-id="31bce-154">*Making run-time errors into compile-time errors.*</span></span> <span data-ttu-id="31bce-155">在其中的代码是保证在运行时 （即用户代码有明确的 bug 中它） 时失败的 100%的情况下，可能需要添加捕获这种情况的编译时错误。</span><span class="sxs-lookup"><span data-stu-id="31bce-155">In a situation where code is 100% guaranteed to fail at runtime (i.e. the user code has an unambiguous bug in it), it may be desirable to add a compile-time error that catches the situation.</span></span>

* <span data-ttu-id="31bce-156">*规范省略。*</span><span class="sxs-lookup"><span data-stu-id="31bce-156">*Specification omission.*</span></span> <span data-ttu-id="31bce-157">当语言规范不会不是专门允许或禁止特定的情况和编译器处理这种情况是不可取 （如果编译器行为是明确的错误，它将为规范 bug，而不规范的方式省略的方式），它可能有必要以阐明规范，并更改编译器行为。</span><span class="sxs-lookup"><span data-stu-id="31bce-157">When the language specification does not specifically allow or disallow a particular situation and the compiler handles the situation in a way that is undesirable (if the compiler behavior was clearly wrong, it would a specification bug, not a specification omission), it may be necessary to clarify the specification and change the compiler behavior.</span></span> <span data-ttu-id="31bce-158">除了常用的影响分析，这种类型的更改并进一步限制为其中更改的影响应该是非常最小，为开发人员带来的好处是非常高的情况下。</span><span class="sxs-lookup"><span data-stu-id="31bce-158">In addition to the usual impact analysis, changes of this kind are further restricted to cases where the impact of the change is considered to be extremely minimal and the benefit to developers is very high.</span></span>

* <span data-ttu-id="31bce-159">*新的功能。*</span><span class="sxs-lookup"><span data-stu-id="31bce-159">*New features.*</span></span> <span data-ttu-id="31bce-160">一般情况下，引入了新功能不应更改现有部件语言规范的或现有的编译器行为。</span><span class="sxs-lookup"><span data-stu-id="31bce-160">In general, introducing new features should not change existing parts of the language specification or the existing behavior of the compiler.</span></span> <span data-ttu-id="31bce-161">在其中引入了一项新功能需要更改现有的语言规范的情况下，此类的兼容性中断才合理影响会非常最小和功能的好处是高。</span><span class="sxs-lookup"><span data-stu-id="31bce-161">In the situation where introducing a new feature requires changing the existing language specification, such a compatibility break is reasonable only if the impact would be extremely minimal and the benefit of the feature is high.</span></span>

* <span data-ttu-id="31bce-162">*安全性。*</span><span class="sxs-lookup"><span data-stu-id="31bce-162">*Security.*</span></span> <span data-ttu-id="31bce-163">在异常情况下，安全问题可能需要兼容性中断，如删除或修改的功能在本质上是不安全，会带来安全风险的用户清除。</span><span class="sxs-lookup"><span data-stu-id="31bce-163">In extraordinary situations, security concerns may necessitate a compatibility break, such as removing or modifying a feature that is inherently insecure and poses a clear security risk for users.</span></span>

<span data-ttu-id="31bce-164">在以下情况下不是可接受引入兼容性中断的原因：</span><span class="sxs-lookup"><span data-stu-id="31bce-164">The following situations are not acceptable reasons for introducing compatibility breaks:</span></span>

* <span data-ttu-id="31bce-165">*不需要或上的行为。*</span><span class="sxs-lookup"><span data-stu-id="31bce-165">*Undesirable or regrettable behavior.*</span></span> <span data-ttu-id="31bce-166">这是合理的但被视为不合理或上以前的语言设计或编译器行为不破坏向后兼容性的理由。</span><span class="sxs-lookup"><span data-stu-id="31bce-166">Language design or compiler behavior which is reasonable but considered undesirable or regrettable in retrospect is not a justification for breaking backward compatibility.</span></span> <span data-ttu-id="31bce-167">必须改为使用该语言不推荐使用过程中，下面，将介绍。</span><span class="sxs-lookup"><span data-stu-id="31bce-167">The language deprecation process, covered below, must be used instead.</span></span>

* <span data-ttu-id="31bce-168">*别的东西。*</span><span class="sxs-lookup"><span data-stu-id="31bce-168">*Anything else.*</span></span> <span data-ttu-id="31bce-169">否则，编译器行为将保持向后兼容。</span><span class="sxs-lookup"><span data-stu-id="31bce-169">Otherwise, compiler behavior remains backwards compatible.</span></span>

### <a name="impact-criteria"></a><span data-ttu-id="31bce-170">影响条件</span><span class="sxs-lookup"><span data-stu-id="31bce-170">Impact Criteria</span></span>

<span data-ttu-id="31bce-171">在考虑是否兼容性中断可能是可接受的用于确定更改的影响可能是多个条件。</span><span class="sxs-lookup"><span data-stu-id="31bce-171">When considering whether a compatibility break might be acceptable, several criteria are used to determine what the impact of the change might be.</span></span> <span data-ttu-id="31bce-172">影响越大，用于接受兼容性越高栏会中断。</span><span class="sxs-lookup"><span data-stu-id="31bce-172">The greater the impact, the higher the bar for accepting the compatibility breaks.</span></span>

<span data-ttu-id="31bce-173">条件是：</span><span class="sxs-lookup"><span data-stu-id="31bce-173">The criteria are:</span></span>

* <span data-ttu-id="31bce-174">更改的范围是什么？</span><span class="sxs-lookup"><span data-stu-id="31bce-174">What is the scope of the change?</span></span> <span data-ttu-id="31bce-175">换而言之，程序数量是否有可能受到影响？</span><span class="sxs-lookup"><span data-stu-id="31bce-175">In other words, how many programs are likely to be affected?</span></span> <span data-ttu-id="31bce-176">多少用户是否有可能受到影响？</span><span class="sxs-lookup"><span data-stu-id="31bce-176">How many users are likely to be affected?</span></span> <span data-ttu-id="31bce-177">编写受此更改影响的代码是如何常见？</span><span class="sxs-lookup"><span data-stu-id="31bce-177">How common will it be to write code that is affected by the change?</span></span>

* <span data-ttu-id="31bce-178">是否存在来获得相同的行为更改之前任何解决办法？</span><span class="sxs-lookup"><span data-stu-id="31bce-178">Do any workarounds exist to get the same behavior prior to the change?</span></span>

* <span data-ttu-id="31bce-179">更改是如何明显？</span><span class="sxs-lookup"><span data-stu-id="31bce-179">How obvious is the change?</span></span> <span data-ttu-id="31bce-180">用户将获得即时反馈，某些内容已更改，或将其程序只需执行以不同方式？</span><span class="sxs-lookup"><span data-stu-id="31bce-180">Will users get immediate feedback that something has changed, or will their programs just execute differently?</span></span>

* <span data-ttu-id="31bce-181">可以更改进行合理的解决在升级过程？</span><span class="sxs-lookup"><span data-stu-id="31bce-181">Can the change be reasonably addressed during upgrade?</span></span> <span data-ttu-id="31bce-182">是否可以编写一个工具，可以找到这种情况中更改的发生是完美的准确性并更改代码以解决此更改？</span><span class="sxs-lookup"><span data-stu-id="31bce-182">Is it possible to write a tool that can find the situation in which the change occurs with perfect accuracy and change the code to work around the change?</span></span>

* <span data-ttu-id="31bce-183">什么是社区的反馈的更改上？</span><span class="sxs-lookup"><span data-stu-id="31bce-183">What is the community feedback on the change?</span></span>

### <a name="language-deprecation"></a><span data-ttu-id="31bce-184">语言不推荐使用</span><span class="sxs-lookup"><span data-stu-id="31bce-184">Language deprecation</span></span>

<span data-ttu-id="31bce-185">随着时间推移，可能会变得弃用语言或编译器的部分。</span><span class="sxs-lookup"><span data-stu-id="31bce-185">Over time, parts of the language or compiler may become deprecated.</span></span> <span data-ttu-id="31bce-186">如前文所述，它不是可接受破坏兼容性，若要删除此类已弃用的功能。</span><span class="sxs-lookup"><span data-stu-id="31bce-186">As discussed previously, it is not acceptable to break compatibility to remove such deprecated features.</span></span> <span data-ttu-id="31bce-187">相反，必须执行以下步骤：</span><span class="sxs-lookup"><span data-stu-id="31bce-187">Instead, the following steps must be followed:</span></span>

1. <span data-ttu-id="31bce-188">提供一种功能，版本中存在*A*必须的 Visual Studio 中，从上弃用的功能和完整的通知之前所做的任何最终不推荐使用决策给定的用户社区中请求的反馈。</span><span class="sxs-lookup"><span data-stu-id="31bce-188">Given a feature that exists in version *A* of Visual Studio, feedback must be solicited from the user community on deprecation of the feature and full notice given before any final deprecation decision is made.</span></span> <span data-ttu-id="31bce-189">不推荐使用过程可能会反转或放弃在用户社区的反馈基础的任意位置。</span><span class="sxs-lookup"><span data-stu-id="31bce-189">The deprecation process may be reversed or abandoned at any point based on user community feedback.</span></span>

2. <span data-ttu-id="31bce-190">完整版 （即，不点发行版） *B*的 Visual Studio 必须释放使用的不推荐使用的使用情况，则发出警告的编译器警告。</span><span class="sxs-lookup"><span data-stu-id="31bce-190">full version (i.e. not a point release) *B* of Visual Studio must be released with compiler warnings that warn of deprecated usage.</span></span> <span data-ttu-id="31bce-191">警告必须在默认情况下，可以关闭。</span><span class="sxs-lookup"><span data-stu-id="31bce-191">The warnings must be on by default and can be turned off.</span></span> <span data-ttu-id="31bce-192">必须清楚地记录弃用功能在产品文档并在 web 上找到。</span><span class="sxs-lookup"><span data-stu-id="31bce-192">The deprecations must be clearly documented in the product documentation and on the web.</span></span>

3. <span data-ttu-id="31bce-193">完整版*C*的 Visual Studio 必须随无法关闭的编译器警告。</span><span class="sxs-lookup"><span data-stu-id="31bce-193">A full version *C* of Visual Studio must be released with compiler warnings that cannot be turned off.</span></span>

4. <span data-ttu-id="31bce-194">完整版*D*的 Visual Studio 必须随后释放与不推荐使用的编译器警告转换为编译器错误。</span><span class="sxs-lookup"><span data-stu-id="31bce-194">A full version *D* of Visual Studio must subsequently be released with the deprecated compiler warnings converted into compiler errors.</span></span> <span data-ttu-id="31bce-195">发行*D*发行版的主流支持阶段 （5 年撰写本文时） 结束后必须出现*A*。</span><span class="sxs-lookup"><span data-stu-id="31bce-195">The release of *D* must occur after the end of the Mainstream Support Phase (5 years as of this writing) of release *A*.</span></span>

5. <span data-ttu-id="31bce-196">最后，版本*E*的 Visual Studio 可能会发布中删除编译器错误。</span><span class="sxs-lookup"><span data-stu-id="31bce-196">Finally, a version *E* of Visual Studio may be released that removes the compiler errors.</span></span>

<span data-ttu-id="31bce-197">不能在此不推荐使用框架中处理的更改将不允许。</span><span class="sxs-lookup"><span data-stu-id="31bce-197">Changes that cannot be handled within this deprecation framework will not be allowed.</span></span>
